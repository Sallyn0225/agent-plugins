# Changesets

Add a Changeset for every user-visible change to a publishable package:

```bash
npx changeset
```

Repository-only maintenance and documentation changes normally do not need a Changeset. Changesets are authoritative for package versions and English changelogs.
